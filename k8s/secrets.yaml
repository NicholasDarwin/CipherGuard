# Create this secret with your actual keys before deploying:
#
#   kubectl create secret generic cipherguard-secrets \
#     --from-literal=GEMINI_API_KEY=<your-key> \
#     --from-literal=SNYK_TOKEN=<your-token>
#
# The template below is a placeholder for reference only.
apiVersion: v1
kind: Secret
metadata:
  name: cipherguard-secrets
type: Opaque
stringData:
  GEMINI_API_KEY: ""
  SNYK_TOKEN: ""
