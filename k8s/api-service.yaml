apiVersion: v1
kind: Service
metadata:
  name: cipherguard-api
  labels:
    app: cipherguard-api
spec:
  selector:
    app: cipherguard-api
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
  type: ClusterIP
